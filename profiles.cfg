# Profile name
NAME=default
# Device to build for
DEVICE=default
# Build type that will appear on the device zip name
BUILDTYPE=default
# Variant you want to build: eng/userdebug/user
VARIANT=userdebug
# Clean build? (y/n)
CLEAN=y
